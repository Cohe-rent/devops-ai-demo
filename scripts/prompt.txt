Generate Terraform code to create a simple local development environment on my machine:
- Use `nginx` as the frontend server (e.g., running on port 8080)
- Use a `Python` backend server (e.g., Flask, running on port 5000)
- No cloud resources, only local setup
- Use the `local` backend in Terraform
- Use the `null_resource` and `local_file` or `external` providers if needed
- Avoid cloud providers like AWS, Azure, or Google
- Do not use non-existent or third-party providers like cloudnativedocker/pador
- Output valid Terraform code that passes `terraform init` and `terraform plan`
