Generate a valid Terraform configuration (`main.tf`) using the `kreuzwerker/docker` provider (version ~> 3.0.2). The configuration must:
- Use the `docker` provider and correctly declare `required_providers`.
- Create a Docker network named `app-network`.
- Run two containers:
  - An Nginx container using the image `nginx:latest`, mapping container port 80 to host port 8180, and connecting it to the `app-network`.
  - A Flask app container using the image `tiangolo/uwsgi-nginx-flask:python3.8`, mapping container port 5000 to host port 8100, mounting the local folder `${abspath(path.module)}/app` to `/app` inside the container, and also connecting to `app-network`.
- Use the correct `volumes`, `ports`, and `networks_advanced` blocks.
- Ensure that volume mount paths are **absolute paths** using `abspath(path.module)`.
- Make sure the entire configuration is valid and will not fail on `terraform init`, `terraform plan`, or `terraform apply`.
