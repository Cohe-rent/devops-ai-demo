Generate a complete Terraform configuration using the kreuzwerker/docker provider to deploy a Flask backend and an Nginx frontend reverse proxy using Docker containers.

The configuration should include:
📦 Provider Block:

terraform {
  required_providers {
    docker = {
      source  = "kreuzwerker/docker"
      version = "~> 3.0.2"
    }
  }
}

provider "docker" {}

🧠 Docker Network:

    Create a Docker network named app-network.

resource "docker_network" "app_network" {
  name = "app-network"
}

🐍 Flask Container:

    Use image tiangolo/uwsgi-nginx-flask:python3.8

    Mount host folder ${path.module}/scripts/app to /app in the container:

volumes {
  host_path      = "${abspath(path.module)}/scripts/app"
  container_path = "/app"
}

    Map container port 5000 to host port 8100

    Attach to the app-network using:

networks_advanced {
  name = docker_network.app_network.name
}

    Use correct depends_on syntax referencing the full network resource.
