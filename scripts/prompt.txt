Generate valid Terraform code using the `kreuzwerker/docker` provider for local Docker setup.

Requirements:
- Use `terraform` block with required provider `kreuzwerker/docker`, version "~> 3.0.2"
- Use `provider "docker"` with no configuration (for local use)
- Create a Docker network named "app-network"
- Create a Docker container named "nginx":
  - Image: "nginx:latest"
  - Connect it to the "app-network" using `networks_advanced` block
  - Use a `ports` block to expose container port 80 to host port 8180
- Create a Docker container named "flask_app":
  - Image: "tiangolo/uwsgi-nginx-flask:python3.8"
  - Mount local folder `./app` to `/app` using a `volumes` block
  - Connect it to the "app-network" using `networks_advanced` block
  - Use a `ports` block to expose container port 5000 to host port 8100

Only output valid Terraform code. Use the correct block structures (`ports`, `volumes`, `networks_advanced`). Do not output explanations.
